<%= form_for(@camp_log) do |f| %>
  <% if @camp_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@camp_log.errors.count, "error") %> prohibited this camp_log from being saved:</h2>

      <ul>
      <% @camp_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :registration_id %><br>
    <%= f.text_field :registration_id %>
  </div>
  <div class="field">
    <%= f.label :log %><br>
    <%= f.text_area :log %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
